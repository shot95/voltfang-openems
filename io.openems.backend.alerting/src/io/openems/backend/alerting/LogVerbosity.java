package io.openems.backend.alerting;

// oEMS
public enum LogVerbosity {
    /**
     * Show no logs.
     */
    NONE,
    /**
     * Show basic infos of Emails that are being send (e.g. recipient and alerting type)
     */
    E_MAIL_ONLY,
    ;
}
